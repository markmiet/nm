using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public interface IDamagedable
{
    // Start is called before the first frame update
    bool AiheutaDamagea(float damagemaara);//tagissa sana damage

}
